extends layout

block content
    div.navbar.navbar-default.navbar-static-top
        div.container
            div.navbar-header
                button.navbar-toggle.collapsed
                    span.sr-only
                        Toggle navigation
                    span.icon-bar
                    span.icon-bar
                    span.icon-bar
                span.navbar-brand
                    EPAM TV

            nav.collapse.navbar-collapse
                ul.nav.navbar-nav
                    li(ng-hide="isLoginPage")
                        a(href="#{{ROUTES.ADMIN_ROOT}}") Edit Slides
                    li(ng-hide="isLoginPage")
                        a(href="#{{ROUTES.ADMIN_ADD}}") Add Slide
                    li(ng-hide="isLoginPage")
                        a(href="#{{ROUTES.ADMIN_TIMELINES}}") Edit Timeline
                    li(ng-hide="isLoginPage")
                        a(href="#{{ROUTES.ADMIN_IMAGES}}") Manage Images
                    li(ng-hide="isLoginPage")
                        a(href="#{{ROUTES.LOGIN_ROOT}}") Log Out
                    li
                        a(href="{{APP_CONST.SLIDESHOW_URL}}") Run Slideshow
    div(ng-view)
    script(src="/js/components.js")
    script(src="/js/app.js")